export enum APIRoutes {
  SIGN_UP = "api/user",
}

export enum Routes {
  LISTINGS = "/",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  MY_LISTINGS = "/my-listings",
  MY_APPLICATIONS = "/my-applications",
  MY_PROFILE = "/my-profile",
}
